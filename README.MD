# WebSocket Chat (Go)

Простой WebSocket-чат, написанный на Go, в формате **pet-проекта**.  
Цель проекта — разобраться с WebSocket в Go, конкурентной моделью (goroutines + channels) и построением аккуратной архитектуры без фреймворков.

Проект задуман как **минимальный, но расширяемый MVP**, который можно реализовать за несколько дней и дальше развивать (комнаты, авторизация, история сообщений и т.д.).

---
## 🚀 Что это за проект

WebSocket сервер, который:
- принимает WS-подключения
- хранит список активных клиентов в памяти
- принимает сообщения от клиента
- рассылает сообщения всем подключённым клиентам
- корректно обрабатывает подключение и отключение

Без базы данных, без REST, без сложной бизнес-логики.

---

## 🧠 Архитектура

Используется классическая для Go модель:

**Hub + Clients**

- **Hub** — центральный объект, который:
  - регистрирует клиентов
  - удаляет клиентов
  - рассылает сообщения
- **Client** — одно WebSocket-соединение:
  - читает сообщения от клиента
  - отправляет сообщения клиенту
- Всё взаимодействие происходит через **channels**
- Мьютексы не используются

Архитектура однопроцессная, состояние хранится в памяти.

---

## 📦 Используемые библиотеки

### Внешние
- `github.com/gorilla/websocket`  
  WebSocket-апгрейд и работа с соединениями

### Стандартная библиотека
- `net/http` — HTTP сервер
- `encoding/json` — сериализация сообщений
- `log` — логирование
- `time` — тайминги, ping/pong
- `context` — управление жизненным циклом

---

## 📁 Структура проекта

```text
ws-chat/
├── cmd/
│   └── server/
│       └── main.go
│
├── internal/
│   ├── config/
│   │   └── config.go
│   │
│   ├── http/
│   │   └── ws_handler.go
│   │
│   ├── ws/
│   │   ├── hub.go
│   │   ├── client.go
│   │   └── message.go
│   │
│   └── logger/
│       └── logger.go
│
├── go.mod
└── README.md
